<%= form_for [playable, Playlist.new] do |f| %>
    <div class="form-group">
        <%= f.text_field :name, class:"form-control", placeholder: "Create a playlist" %>
    </div>
    <%= f.submit class: "btn btn-primary" %>
<% end %>