<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">MusicApp</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
            <% if logged_in? %>
                <a class="nav-link active" aria-current="page" href="<%= user_path(current_user.id)%>">Home</a>
            <%else%>
                <a class="nav-link active" aria-current="page" href="/">Home</a>
            <%end%>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/playlists">My Playlists</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/songs">Songs</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/artists">Artists</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/albums">Albums</a>
        </li>
      </ul>

        <div class="d-flex align-items-center">

            <% if logged_in? %>
                <div class="text-white"> Hi, <%= current_user.username %> </div> &nbsp; &nbsp; &nbsp;
            <div>
            <%= button_to "Logout", '/logout', method: :post, class: "btn btn-outline-light"%>
            </div>
            <%else%>
                <%= button_to "Sign In", '/login', method: :get, class: "btn btn-outline-light" %> &nbsp; &nbsp; 
                <%= button_to "Create account", '/users/new', method: :get, class: "btn btn-outline-success" %> 
            <%end%> 
        </div>
    </div>
  </div>
</nav>